db:
  image: mysql
  ports:
    - "13306:3306"
  environment:
    MYSQL_ROOT_PASSWORD: root

web:
  # build: dakotalightning/koda-rails
  image: dakotalightning/koda-rails
  ports:
    # "out/in"
    - "80:3000"
  links:
    - db
  command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s puma -b 0.0.0.0"
  environment:
    SECRET_KEY_BASE: fdea18ac839a1607fe8193fefb8b23
