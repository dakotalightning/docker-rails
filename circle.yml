machine:
  services:
    - docker

dependencies:
  pre:
  - sudo pip install docker-compose
#    - docker-compose -f ./docker-production.yml up -d

test:
  override:
  - docker-compose -f docker-production.yml up --no-deps web
