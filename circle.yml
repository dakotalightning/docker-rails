machine:
  services:
    - docker

dependencies:
  override:
    - pip install docker-compose

test:
  pre:
    - docker-compose up -d -f ./docker-production.yml
  # pre:
  #   - docker-compose run -d web
