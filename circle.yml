machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install -U docker-compose

test:
  pre:
    - docker-compose pull
    - docker-compose up -d
  override:
    - docker-compose run -d web
