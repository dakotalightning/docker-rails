machine:
  services:
    - docker

dependencies:
  pre:
  - sudo pip install docker-compose
#    - docker-compose -f ./docker-production.yml up -d

test:
  override:
  - docker-compose -f docker-production.yml up -d web
  - docker-compose run web RACK_ENV=test bundle exec rake db:create db:schema:load --trace
