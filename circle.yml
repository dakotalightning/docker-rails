machine:
  services:
    - docker

dependencies:
  pre:
    - sudo pip install docker-compose
    - docker-compose up -d -f ./docker-production.yml

# test:
  # pre:
  #   - docker-compose run -d web
